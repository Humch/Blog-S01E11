import React from 'react';

import { Button } from '@the-sleeping-dog/react-components';

const SubmitButton: React.FC = () => {
  return (
    <Button color="is-primary" type="submit">
      Sign in
    </Button>
  );
};

export default SubmitButton;
